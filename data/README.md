# Data policy

- `public/` contains only safe synthetic fixtures or explicitly redistributable samples.
- `private/` is ignored by Git and is the only location for authorized local materials.
- Never commit raw resumes, names, email addresses, phone numbers, internal JDs, API keys, or human Gold containing identifiers.
- A public GitHub repository must use synthetic or separately authorized examples.
